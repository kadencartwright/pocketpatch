# @pocketpatch/daemon

Effect daemon package for lifecycle, HTTP/API serving, and coordination between the CLI, storage, and phone UI.

Implementation is intentionally deferred until tests define the behavior.

